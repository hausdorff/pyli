name:             pyle
version:          0.1
build-type:       Simple
cabal-version:    >= 1.2

library
  hs-source-dirs: src
  build-depends:  base == 4.5.*, HUnit, derp == 0.1.1

executable        pyle
  main-is:        Main.hs
  hs-source-dirs: src
  ghc-options:    -Wall
